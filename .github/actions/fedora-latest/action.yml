name: "Fedora Latest"
description: "Run a command on a pre-prepared Fedora container with Rust installed"
inputs:
  command:
    description: "Path to command to be ran"
    required: true
runs:
  using: "docker"
  image: "Dockerfile"
  args:
    - ${{ inputs.command }}
